class Textmass {

    constructor() {
        //this needs to be a list of strings. Each element corresponds to a line.
        this.textLines = [];
    }

    addLines (line) {
        this.textLines.push(line);
    }

}